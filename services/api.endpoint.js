const BASE_ENDPOINT = '/api';

export const ENDPOINT = {
    // Auth endpoints
    LOGIN_GOOGLE: `${BASE_ENDPOINT}/auth/google-login`, // Google login endpoint
    REFRESH_TOKEN: `${BASE_ENDPOINT}/auth/refresh-token`,
}